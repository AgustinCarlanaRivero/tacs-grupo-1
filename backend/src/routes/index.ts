import { Router } from 'express'

const router = Router()

// Los módulos se irán registrando acá a medida que se implementen:
// import authRoutes from '../modules/auth/auth.routes.ts'
// router.use('/auth', authRoutes)

export default router
