# Plantilla de variables de entorno. Copiar a `.env.dev` y `.env.prod`
# (ambos ignorados por git) y completar los valores reales.

NODE_ENV=
PORT=
URL=

# Auth0 API configuration
AUTH0_ISSUER_BASE_URL=
AUTH0_AUDIENCE=
